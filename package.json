{
  "name": "cybercrime-bg",
  "version": "1.0.0",
  "private": true,
  "description": "cybercrime.bg build tooling: precompiles JSX and Tailwind CSS ahead of time so the site can drop 'unsafe-inline'/'unsafe-eval' from its CSP.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "tailwindcss": "^3.4.17"
  }
}
